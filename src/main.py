from inputs.tokens import primary_token, secondary_tokens, random_token, make_headers
from modules.get_followers_list import get_followers_list

token = secondary_tokens[""]
header = make_headers(token)

